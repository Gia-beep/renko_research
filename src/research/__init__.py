"""Reusable research primitives (factor evaluation + Qlib data loading).

These are pure-ish helpers extracted from the entry scripts so that both the
baseline brick-alpha study and the signal-enhancement study share one tested
implementation, per the repo's ``src/`` = library, ``scripts/`` = orchestration
split.
"""
